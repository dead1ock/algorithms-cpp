PROJECT(algorithms-cpp)
CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

INCLUDE(Sources.cmake)

ADD_LIBRARY(algorithms-cpp ${MODULE_SOURCES})
SET_TARGET_PROPERTIES(algorithms-cpp PROPERTIES LINKER_LANGUAGE CXX)
