PROJECT(tests C CXX)
CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

enable_testing()

INCLUDE_DIRECTORIES(
	${CMAKE_SOURCE_DIR}/src
)

LINK_DIRECTORIES(
)

INCLUDE(Sources.cmake)

ADD_EXECUTABLE(tests ${MODULE_SOURCES})
TARGET_LINK_LIBRARIES(tests algorithms-cpp)
